<UserControl x:Class="DidoGest.UI.Views.Magazzino.MovimentiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Movimenti Magazzino" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="300" materialDesign:HintAssist.Hint="Cerca..." 
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"
                     TextChanged="SearchBox_TextChanged"/>
            <Button Content="Nuovo Movimento" Click="BtnNuovo_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
            <Button Content="Elimina" Click="BtnElimina_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="MovimentiDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False"
                  MouseDoubleClick="MovimentiDataGrid_MouseDoubleClick"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding DataMovimento, StringFormat=dd/MM/yyyy}" Width="110"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoMovimento}" Width="100"/>
                <DataGridTextColumn Header="Articolo" Binding="{Binding ArticoloCodice}" Width="110"/>
                <DataGridTextColumn Header="Descrizione" Binding="{Binding ArticoloDescrizione}" Width="260"/>
                <DataGridTextColumn Header="Mag" Binding="{Binding MagazzinoCodice}" Width="80"/>
                <DataGridTextColumn Header="Q.tà" Binding="{Binding Quantita, StringFormat=N2}" Width="90"/>
                <DataGridTextColumn Header="Costo" Binding="{Binding CostoUnitario, StringFormat=N2}" Width="90"/>
                <DataGridTextColumn Header="Doc" Binding="{Binding NumeroDocumento}" Width="120"/>
                <DataGridTextColumn Header="Causale" Binding="{Binding Causale}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Movimenti: " FontWeight="Bold"/>
            <TextBlock x:Name="TotaleMovimenti" Text="0"/>
        </StackPanel>
    </Grid>
</UserControl>
