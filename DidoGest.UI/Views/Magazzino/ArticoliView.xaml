<UserControl x:Class="DidoGest.UI.Views.Magazzino.ArticoliView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gestione Articoli" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="300" materialDesign:HintAssist.Hint="Cerca articolo..." 
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"
                     TextChanged="SearchBox_TextChanged"/>
            <Button Content="Nuovo Articolo" Click="BtnNuovo_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
            <Button Content="Modifica" Click="BtnModifica_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
            <Button Content="Elimina" Click="BtnElimina_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="ArticoliDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False"
                  MouseDoubleClick="ArticoliDataGrid_MouseDoubleClick"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Codice" Binding="{Binding Codice}" Width="120"/>
                <DataGridTextColumn Header="Descrizione" Binding="{Binding Descrizione}" Width="300"/>
                <DataGridTextColumn Header="UM" Binding="{Binding UnitaMisura}" Width="80"/>
                <DataGridTextColumn Header="Prezzo" Binding="{Binding PrezzoVendita, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Costo" Binding="{Binding PrezzoAcquisto, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Categoria" Binding="{Binding Categoria}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Totale articoli: " FontWeight="Bold"/>
            <TextBlock x:Name="TotaleArticoli" Text="0"/>
            <TextBlock Text=" | Articoli attivi: " FontWeight="Bold" Margin="20,0,0,0"/>
            <TextBlock x:Name="ArticoliAttivi" Text="0"/>
        </StackPanel>
    </Grid>
</UserControl>
