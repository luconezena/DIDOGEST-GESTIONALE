<UserControl x:Class="DidoGest.UI.Views.Contabilita.PrimaNotaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Prima Nota" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <DatePicker x:Name="DataDa" Width="150" materialDesign:HintAssist.Hint="Da data" Margin="0,0,10,0"/>
            <DatePicker x:Name="DataA" Width="150" materialDesign:HintAssist.Hint="A data" Margin="0,0,10,0"/>
            <Button Content="Cerca" Click="BtnCerca_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
            <Button Content="Nuova Registrazione" Click="BtnNuovo_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
            <Button Content="Elimina" Click="BtnElimina_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="PrimaNotaDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False"
                  MouseDoubleClick="PrimaNotaDataGrid_MouseDoubleClick"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding DataRegistrazione, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="N.Reg" Binding="{Binding NumeroRegistrazione}" Width="80"/>
                <DataGridTextColumn Header="Causale" Binding="{Binding Causale}" Width="200"/>
                <DataGridTextColumn Header="Descrizione" Binding="{Binding Descrizione}" Width="300"/>
                <DataGridTextColumn Header="Dare" Binding="{Binding TotaleDare, StringFormat=C2}" Width="120"/>
                <DataGridTextColumn Header="Avere" Binding="{Binding TotaleAvere, StringFormat=C2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Totale registrazioni: " FontWeight="Bold"/>
            <TextBlock x:Name="TotaleRegistrazioni" Text="0"/>
            <TextBlock Text=" | Saldo: " FontWeight="Bold" Margin="20,0,0,0"/>
            <TextBlock x:Name="Saldo" Text="€ 0,00"/>
        </StackPanel>
    </Grid>
</UserControl>
